# 清洗规则在读取时应用，原文不落库改写

导入的消息内容以原始形式存入来源存储；清洗规则（记忆空间内按顺序执行的正则列表，保留/去掉两种模式）只在**读取**时套用——消息展示与填表任务输入使用清洗后的内容，而数据库中的原始内容始终不变。

选择读取时而非导入时变换，是因为：规则是易试错的配置，读取时应用使规则修改**追溯生效**，无需重新导入 JSONL；且原始内容作为证据（reference 模式）的最终真相，领域上不应被破坏。代价是每次读取需执行规则（填表按块读取、量级小，可编译缓存），以及展示内容与存储内容不一致——这是刻意为之。

_Status_: accepted
