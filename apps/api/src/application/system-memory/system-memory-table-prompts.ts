/**
 * v4 模板提示词（2026-08-06 数据库质量审查后重构，v4 世界状态表修订）：
 *
 * - 事件归位：事件本体只进剧情表（plots）；其他表只写各自视角的稳定状态，
 *   跨表关联用引用字段，禁止复制事件过程文本；
 * - 每字段固定句式：「本字段只记录……不记录……」，长度上限与字段 maxChars 一致；
 * - 状态字段强制覆盖语义（新状态替换旧状态）；plots.details 为**追加式摘要**
 *   （保留已有事实、追加新进展，允许润色措辞但不得删事实——details 是未来 RAG/搜索的语料）；
 * - 时间坐标用稳定相对坐标「第 N 天·时段」；v4 起由**世界状态表**（story_state）承载
 *   剧情时钟（时间/天气/当日着装/节日），plots.time_hint 参照其 current_time，
 *   禁止「今天/当天/次日」等相对词。
 */
export const SYSTEM_TABLE_PROMPTS = {
  characters:
    "记录会话中具有独立身份且值得持续记忆的人物，包括用户扮演的人物。相同身份只维护一条记录，无法确认身份时不擅自合并。只写证据支持的内容。人物事件本身不要记录在本表（事件只进剧情表），本表只维护身份、稳定特质与此刻状态。",
  relationships:
    "每对人物维护一条关系记录，创建前查询两种人物顺序以避免重复。本表只记录关系本身的稳定定性、里程碑事实与此刻状态；具体事件过程不要写进本表（事件只进剧情表）。",
  locations:
    "记录具有可辨识身份、可能再次出现或影响剧情的地点。本表只写地点的固定属性与此刻正在发生什么；事件过程不要写进本表（事件只进剧情表）。",
  items:
    "记录具有剧情作用、归属意义或需要持续追踪的物品。本表只写物品的固定属性、归属与当前状态；使用过程与事件细节不要写进本表（事件只进剧情表）；当日穿着、临时配饰不要写进本表（进世界状态表的当日着装字段），本表只记录具有持续追踪意义的物品。",
  plots:
    "一条记录表示一个能够跨越多个场景持续发展的剧情事件。本表是事件唯一归位处：事件本体（发生了什么、进展到什么程度）只记录在这里；其他表禁止重复记录同一事件。详情采用追加式摘要：保留已有事实，追加本轮新进展；允许润色措辞使总长不超过字段上限，但不得删除已记录的事实；禁止全文转写对话。",
  foreshadowing:
    "记录已经出现、尚未闭环且可能在后续触发或回收的叙事线索。只立重要且有回收价值的线索，琐碎插曲不要立伏笔。本表只写线索本身与回收信息；事件经过不要写进本表（事件只进剧情表）。",
  todos:
    "记录角色或剧情明确提出、尚需执行的行动事项。已完成或已放弃的事项清空详情。已在剧情表记录过的事件，本表只写待执行的动作本身，不重复事件背景。",
  storyState:
    "本表维护剧情世界的当前状态快照，是剧情时钟的唯一载体。全表仅一条记录（名称固定为『世界状态』），更新时覆盖旧值，绝不新建第二条。处理每块消息时，若时间、地点、天气、服装有任何变化，必须先更新本表；其他表（尤其 plots.time_hint）的时间坐标一律以本表 current_time 为准。",
} as const;

export const SYSTEM_FIELD_PROMPTS = {
  currentStatus:
    "本字段只保留此刻仍成立的状态，最多 3 条；新状态覆盖旧状态；禁止事件叙述与过程描写。",
  relationshipCharacterA: "引用人物表中的一方；创建前同时查询两种人物顺序。",
  relationshipCharacterB: "引用人物表中的另一方；不可与人物 A 指向同一记录。",
  relatedCharacters: "只引用确实参与、负责或受到影响的人物。",
  relatedLocations: "只引用能够明确识别且与该记录有关的地点。",
  relatedItems: "只引用确实位于此处或与此处持续相关的物品。",
  holder: "引用证据明确指向的持有者或所属人物，不作所有权推断。",
  currentLocation: "引用能够明确识别的地点，不为模糊位置创建记录。",
  plotStatus: "根据证据选择进行中、暂停、已解决或已放弃，不自行预测。",
  foreshadowingStatus: "根据证据选择埋设中、已触发、已回收或已放弃。",
  todoStatus: "根据证据选择待处理、进行中、已完成或已放弃。",
  timeHint:
    "本字段只记录事件发生的时间坐标，格式「第 N 天·时段」（如：第一天清晨、第二天傍晚）。天数一律参照世界状态表的 current_time（跨天先推进状态表）；禁止使用『今天/当天/次日』等相对词；无信息则留空。",
  storyTime:
    "本字段是剧情时钟，只写时间与时段，格式「第 N 天·时段」（如：第一天清晨、第二天傍晚）。填写规则：消息有明确时间表述（如『第二天早上』）时按表述填写；无明确表述但剧情延续时沿用上一值的天数；尚无记录时从「第一天」开始；跨入新的一天时天数 +1。不写地点、事件、天气（分别进当前地点、剧情、天气字段）。",
  storyLocation:
    "本字段只记录主要角色当前所在（如：宿舍客厅、超市收银台）；多人分处时简写各自位置；场景切换时同步更新；无信息则留空。",
} as const;
