# Apps

该上下文描述应用层：`apps/api` 与 `apps/web` 是一对，web 前端通过 HTTP 与 api 对话，共享同一套应用层词汇。领域词汇在 `core/CONTEXT.md` 中定义，此处不重复。

## Language

**清洗规则**：
记忆空间内按顺序执行的正则规则，作用于导入消息的内容，以「保留」或「去掉」模式改写消息内容，影响消息展示与填表任务输入；存储的原始消息内容不被改写。
_Avoid_: 正则脚本、过滤规则、文本替换
